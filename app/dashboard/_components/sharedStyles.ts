// 16px on mobile is a hard floor — below that iOS zooms the page on focus.
export const inputCls = 'w-full bg-transparent border border-ink/15 rounded-sm px-3 py-2.5 font-mono text-[16px] md:text-[13px] text-ink outline-none focus:border-[var(--accent-ink)]/60 transition-colors';
export const labelCls = 'block font-mono text-[10px] uppercase tracking-[2px] mb-1.5 font-semibold text-ink/50';
